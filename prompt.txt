You are working on an existing Python project located in @beautifulMentionfolder. This project currently evaluates fault tolerance, compromise resistance, and latency by sending a single text input across all priority levels.

Your task is to extend (NOT replace or break) the existing system by introducing structured, realistic input data and a new coding scheme.

Objectives
1. Patient Document Simulation Layer
Introduce a new module to generate and handle patient documents (text-based).
Each document must include:
patient_name: randomly generated realistic name
disease: randomly selected illness
Maintain a configurable dataset of diseases categorized into:
LOW, MEDIUM, HIGH priority
Generate multiple documents per run (configurable size, e.g., N=50, 100, etc.)
2. Integration with Existing Pipeline
Extend the current evaluation pipeline to support:
Existing single-text input (unchanged)
New patient document inputs (additional path)
Ensure all metrics are computed for both modes:
Fault tolerance
Compromise resistance
Latency
Provide a toggle/config flag:
mode = "single" (existing)
mode = "patient_docs" (new)
Ensure backward compatibility and no regression.
3. Disease Coding System Upgrade
Current format: 21_HA, 15_fever
Add a new standardized coding scheme:
Format: <number>_<id> (e.g., 21_1, 15_2)
Requirements:
Each unique disease maps to a unique numeric ID
Maintain a persistent mapping dictionary
Support bidirectional lookup:
disease → code
code → disease
Preserve legacy format compatibility (do not remove old codes).
4. Architectural Constraints
Do NOT delete or modify core logic unnecessarily.
Add new functionality via:
New modules (e.g., patient_data.py, disease_mapper.py)
Wrapper/adaptor functions
Follow clean code principles:
Modular design
Reusable components
Clear function boundaries
Ensure minimal intrusion into existing files.
5. Output & Observability
Update logs/prints to clearly differentiate:
Single-text execution
Patient-document execution
Provide structured output for each document:
Patient name
Disease
Priority level
Assigned code
Metrics results
6. Deliverables
Updated Python code (fully working, no regressions)
New modules:
Patient document generator
Disease-code mapper
Sample generated patient dataset
Integration points explained (where and how hooked into pipeline)
Example execution outputs for both modes
Inline comments + brief developer notes